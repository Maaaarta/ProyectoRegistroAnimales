import { inicialitzarApp } from './viewmodel.js';
// importa la funcion para iniciar la app

document.addEventListener('DOMContentLoaded', () => {
    inicialitzarApp(); // ejecuta la funcion cuando el dom este cargado
});
